{
  "name": "@waterlily/server",
  "version": "0.0.0",
  "private": true,
  "description": "Loopback-only persistence and provider service for WaterLily",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm build && node --env-file-if-exists=../../.env dist/main.js",
    "lint": "eslint src test vitest.config.ts",
    "start": "node --env-file-if-exists=../../.env dist/main.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@waterlily/api-contract": "workspace:*",
    "@waterlily/database": "workspace:*",
    "@waterlily/domain": "workspace:*",
    "@waterlily/providers": "workspace:*",
    "@waterlily/workflows": "workspace:*"
  }
}
